# Free EmailJS + Zoho Mail SMTP (ednetlearn.in)
# Create a free EmailJS account, connect Zoho SMTP, create a template with:
#   {{to_email}}  {{otp_code}}
# Then fill these (never commit real values — use GitHub Secrets / local .env.local):
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
