# Copilot

Canonical rules live in `.github/copilot-instructions.md`.
Read that file first. Do not copy this manifesto into the UI.
If this file and the rules file disagree, the rules file wins.

On-screen names: `public/labels.txt` field 3.
